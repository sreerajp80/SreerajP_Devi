# Workspace Plan: Phase 1 — Core Foundation & UI Setup
**Status:** completed

## Issue/Goal
We need to implement Phase 1 of the Nine Durgas (Navadurga) devotional application:
- Initialize the Flutter project with package name `in.sreerajp.devi` and project name `sreerajp_devi`.
- Configure dependencies in `pubspec.yaml` (Riverpod, audioplayers, package_info_plus, google_fonts).
- Set up core directory structure (`lib/core/...`, `lib/models/`, `lib/repositories/`, `lib/screens/`, `lib/widgets/`).
- Define Devotional theme and color locking (Rakta Red, Swarna Gold, Haridra Background, Mayura Accent).
- Implement custom Material 3 app theme.

## Files to be Created/Changed
- `pubspec.yaml` (Modified/Created)
- `lib/main.dart` (Modified/Created)
- `lib/app.dart` (Created)
- `lib/core/constants/app_colors.dart` (Created)
- `lib/core/theme/app_theme.dart` (Created)

## Plan
1. Run `flutter create` command to initialize the project structure.
2. Edit `pubspec.yaml` to set dependencies:
   - `flutter_riverpod: ^2.5.1` (or latest stable)
   - `audioplayers: ^6.0.0` (or latest stable)
   - `package_info_plus: ^8.0.0` (or latest stable)
   - `google_fonts: ^6.2.1` (or latest stable)
3. Run `flutter pub get`.
4. Create the required directory structure.
5. Create `lib/core/constants/app_colors.dart` containing:
   - `raktaRed = Color(0xFF8B0000)`
   - `swarnaGold = Color(0xFFD4AF37)`
   - `haridraBackground = Color(0xFFFDF6E2)`
   - `mayurapeacockAccent = Color(0xFF0F5257)`
6. Create `lib/core/theme/app_theme.dart` with a custom `ThemeData` setup applying these colors as Material 3 colors.
7. Create `lib/app.dart` with a basic `MaterialApp` configuring routes/themes.
8. Create `lib/main.dart` running the app inside a `ProviderScope`.
9. Verify using `flutter analyze` and `flutter test`.
