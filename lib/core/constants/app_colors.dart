import 'package:flutter/material.dart';

/// Locked color constants for the Devi (Navadurga) devotional application.
/// Color palette is derived from traditional sacred themes (Rakta Red, Swarna Gold, etc.).
class AppColors {
  AppColors._();

  /// Primary color: Rakta Red (Blood-Red Lotus)
  static const Color raktaRed = Color(0xFF8B0000);

  /// Secondary color: Swarna Gold (Sacred Gold accents)
  static const Color swarnaGold = Color(0xFFD4AF37);

  /// Background color: Haridra/Ghee Background (Warm Turmeric/Ghee White)
  static const Color haridraBackground = Color(0xFFFDF6E2);

  /// Accent color: Mayura Peacock Accent (Peacock Blue/Green)
  static const Color mayuraPeacock = Color(0xFF0F5257);

  /// Neutral dark color for primary text
  static const Color textDark = Color(0xFF2C2520);

  /// Neutral light/soft gold color for card background highlights
  static const Color cardBackground = Color(0xFFFFFDF9);
}
